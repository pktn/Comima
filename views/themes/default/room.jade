extends layout

block content
	.color-line
	.wrap
		header
			include status_dropdown
			include rooms_dropdown
			a.button-circle.thread.fancybox(href='#thread')
			strong
					span
						i
			#thread.modal.modal-thread(style='display: none')
				h2 スレッド新規作成
					.modal-content
						ほげほげ
					.thread-detail
						input(type='text', name='', value='')
					.modal-footer
						a.button.post-thread(href='#') スレッドを立てる

			a.button-circle.lock(href='#', style="display:none;")
				strong
					span
						i
		sidebar
			include matching_list
		sidebar
			include chat_list
			include files_list
  
		.content(role='main')
			.thread
				// thread-content
				.history
					//historical submits
				.current
					//current submits
			// .thread 

			.line-thread
			.line-thread.second

			.chat
				// chat-content
				.history
					//historical submits
				.current
					//current submits
			// .chat 

			.line-chat
			.line-chat.second

			.chat-input
				input.text(type='text', name='', value='')
				// .chat-input

	// .wrap

	span#room_name(style="display:none")= room.name
	span#username(style="display:none")= user.nickname

	include templates/chat_tmpl
	include templates/thread_tmpl

	script(defer, src="/js/libs/fancy-box/jquery.fancybox.pack.js")
	script(defer, src="/js/plugins.js")
	script(defer, src="/js/libs/ICanHaz.min.js")
	script(src="/socket.io/socket.io.js")

	script(defer, src="/js/script.js")              
